{
  "name": "basic-gpu-inference",
  "version": "0.0.1",
  "description": "Fractal + GCP Live System sample — a GPU VM that serves a self-hosted LLM (vLLM + Qwen2.5-32B) as an OpenAI-compatible endpoint",
  "main": "build/src/gcp.js",
  "types": "build/src/gcp.d.ts",
  "files": [
    "build/src"
  ],
  "license": "AGPL-3.0-only",
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "gts": "^7.0.0",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@fractal_cloud/sdk": "^2.4.0"
  }
}
