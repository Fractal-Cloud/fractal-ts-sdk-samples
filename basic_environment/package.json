{
  "name": "basic-environment",
  "version": "0.0.1",
  "description": "Environment management sample — init a management + operational environment (cloud agents, secrets, CI/CD), then deploy a LiveSystem into the operational environment",
  "main": "build/src/azure.js",
  "types": "build/src/azure.d.ts",
  "files": [
    "build/src"
  ],
  "license": "AGPL-3.0-only",
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "dependencies": {
    "@fractal_cloud/sdk": "^2.3.3"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "gts": "^7.0.0",
    "typescript": "^5.6.3"
  }
}
