{
  "name": "basic-observability",
  "version": "0.0.1",
  "description": "Fractal + CaaS Live System sample — basic Observability (Prometheus, Jaeger, Elastic)",
  "main": "build/src/index.js",
  "types": "build/src/index.d.ts",
  "files": [
    "build/src"
  ],
  "license": "AGPL-3.0-only",
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "gts": "^7.0.0",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@fractal_cloud/sdk": "^2.3.5"
  }
}
