{
  "name": "app-with-identity",
  "version": "0.0.1",
  "description": "Fractal Live System sample — a Workload behind an IdentityProvider (AWS Cognito / Azure Entra External ID) with a relational database, cross-cloud",
  "main": "build/src/azure.js",
  "license": "AGPL-3.0-only",
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "gts": "^7.0.0",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@fractal_cloud/sdk": "^2.3.3"
  }
}
