{
  "name": "basic-container-platform",
  "version": "0.0.1",
  "description": "Fractal + Live System sample — NetworkAndCompute domain (VirtualNetwork + Subnet + SecurityGroup + ContainerPlatform + Workloads)",
  "main": "build/src/index.js",
  "types": "build/src/index.d.ts",
  "files": [
    "build/src"
  ],
  "license": "AGPL-3.0-only",
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "dependencies": {
    "@fractal_cloud/sdk": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "gts": "^7.0.0",
    "typescript": "^5.6.3"
  }
}
